/**
 * Code-level sentinel — NOT a UI string. Used as the default `display_name`
 * for new profiles and as a discriminator in onboarding redirect logic and
 * the people-grid filter. Never translate this value.
 */
export const ANONYMOUS = "Anonymous";